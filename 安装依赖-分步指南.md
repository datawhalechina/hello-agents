# Hello-Agents 依赖安装分步指南

## ⚠️ 问题说明

如果遇到 `resolution-too-deep` 错误，这是因为依赖关系太复杂。请使用**分步安装**方法。

---

## 🚀 推荐安装方式（分步安装）

### 方法 1: 分步安装（推荐）

```bash
# 步骤 1: 安装核心依赖（必需）
pip install -r requirements-core.txt

# 步骤 2: 安装扩展依赖（Web、工具等）
pip install -r requirements-step2.txt

# 步骤 3: 安装 Agent 框架（第六章需要）
pip install -r requirements-step3.txt

# 步骤 4: 安装可选依赖（Jupyter、测试等）
pip install -r requirements-step4.txt
```

### 方法 2: 精简安装（最快）

如果只需要学习基础章节（1-7章），使用精简版：

```bash
pip install -r requirements-minimal.txt
```

### 方法 3: 使用 uv（最快，推荐）

`uv` 是新一代 Python 包管理器，速度极快且能解决复杂依赖：

```bash
# 安装 uv
pip install uv

# 使用 uv 安装（会自动解决依赖冲突）
uv pip install -r requirements.txt
```

---

## 📋 按章节安装（最灵活）

### 基础章节（1-4章）

```bash
pip install openai>=1.0.0 requests>=2.31.0 tavily-python>=0.3.0 python-dotenv>=1.0.0
```

### 第5章（低代码平台）

无需额外依赖，使用 Web 平台。

### 第6章（框架实践）

**只安装需要的框架**：

```bash
# LangGraph
pip install langgraph>=1.0.0 langchain-openai>=0.3.33

# 或 CAMEL
pip install camel-ai>=0.2.75

# 或 AutoGen
pip install autogen-agentchat autogen-ext[openai,azure] streamlit>=1.28.0

# 或 AgentScope
pip install agentscope>=1.0.2
```

### 第7-9章（HelloAgents 框架）

```bash
# 先安装核心
pip install -r requirements-core.txt

# 如果需要完整功能
pip install "hello-agents[all]>=0.2.7"
```

### 第10章（通信协议）

```bash
pip install -r requirements-core.txt
pip install fastmcp>=2.0.0
```

### 第11章（Agentic RL）

```bash
pip install -r requirements-core.txt
# PyTorch 需要单独安装（见下方）
```

### 第12章（评估）

```bash
pip install -r requirements-core.txt
pip install pytest>=7.4.0
```

### 第13-15章（实战案例）

```bash
pip install -r requirements-core.txt
pip install -r requirements-step2.txt
```

---

## 🔧 解决依赖冲突的技巧

### 技巧 1: 使用版本上限

如果某个包版本冲突，添加版本上限：

```bash
pip install "package>=1.0.0,<2.0.0"
```

### 技巧 2: 忽略已安装的包

如果遇到冲突，可以忽略已安装的包：

```bash
pip install --ignore-installed package-name
```

### 技巧 3: 使用 --no-deps

先安装主包，再单独安装依赖：

```bash
pip install --no-deps hello-agents
pip install openai requests python-dotenv
```

### 技巧 4: 升级 pip

确保使用最新版本的 pip：

```bash
python -m pip install --upgrade pip
```

---

## 🐛 常见问题解决

### Q1: 仍然遇到 resolution-too-deep

**解决方案**:
1. 使用 `uv` 工具（最快）
2. 分步安装（见上方）
3. 只安装需要的章节依赖

### Q2: 某个框架安装失败

**解决方案**:
1. 跳过该框架，先学习其他章节
2. 查看该框架的官方文档
3. 使用 Docker 环境（隔离依赖）

### Q3: PyTorch 安装很慢

**解决方案**:
```bash
# 使用国内镜像
pip install torch torchvision torchaudio -i https://pypi.tuna.tsinghua.edu.cn/simple

# 或使用 PyTorch 官方镜像
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
```

### Q4: Jupyter 安装后无法启动

**解决方案**:
```bash
# 重新安装 ipykernel
pip install --force-reinstall ipykernel

# 添加到 Jupyter
python -m ipykernel install --user --name=venv_hello_agents
```

---

## ✅ 验证安装

安装完成后运行：

```bash
python test_installation.py
```

---

## 📊 安装时间参考

| 安装方式 | 预计时间 | 适用场景 |
|---------|---------|---------|
| 精简版 | 2-5分钟 | 快速开始，学习基础 |
| 分步安装 | 10-20分钟 | 完整学习，避免冲突 |
| 完整安装 | 15-30分钟 | 一次性安装所有依赖 |
| uv 安装 | 5-10分钟 | 最快，推荐 |

---

## 💡 推荐工作流

### 第一次安装（推荐）

```bash
# 1. 安装核心依赖
pip install -r requirements-core.txt

# 2. 验证安装
python test_installation.py

# 3. 根据学习进度，逐步添加其他依赖
```

### 学习特定章节时

```bash
# 查看该章节需要什么依赖
# 然后只安装需要的包
```

---

## 🎯 总结

**最佳实践**：
1. ✅ 先安装核心依赖（requirements-core.txt）
2. ✅ 验证安装是否成功
3. ✅ 根据学习进度逐步添加其他依赖
4. ✅ 遇到冲突时使用分步安装或 uv

**避免**：
1. ❌ 一次性安装所有依赖（容易冲突）
2. ❌ 忽略版本约束（可能导致不兼容）
3. ❌ 在系统 Python 中安装（使用虚拟环境）

---

祝安装顺利！🎉

