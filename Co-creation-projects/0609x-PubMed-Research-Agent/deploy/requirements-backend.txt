# Backend-only dependencies for the Docker image (slimmer than requirements.txt).
# The full requirements.txt (frontend + testing) is for local development.

# --- Backend Core (FastAPI) ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

# --- Database ---
sqlalchemy>=2.0.36
alembic>=1.14.0
aiosqlite>=0.20.0
asyncpg>=0.30.0

# --- Background Jobs ---
celery[redis]>=5.4.0

# --- Validation & Config ---
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.1

# --- PubMed ---
biopython>=1.84

# --- Vector Store / RAG ---

# --- Knowledge Graph ---
neo4j>=5.20.0

# --- HTTP Client ---
httpx>=0.28.0
