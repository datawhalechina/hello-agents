# 虚拟环境使用指南

## ✅ 当前状态

虚拟环境已创建并配置完成：
- **位置**: `F:\Hello_Agents\hello-agents\venv_hello_agents`
- **Python 版本**: 3.11.5
- **核心依赖**: 已安装 ✅

---

## 🚀 激活虚拟环境

### Windows PowerShell

```powershell
# 激活虚拟环境
.\venv_hello_agents\Scripts\Activate.ps1

# 如果遇到权限错误，先执行：
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Windows CMD

```cmd
venv_hello_agents\Scripts\activate.bat
```

### macOS/Linux

```bash
source venv_hello_agents/bin/activate
```

### 验证激活成功

激活后，命令行提示符前会显示 `(venv_hello_agents)`：

```powershell
(venv_hello_agents) PS F:\Hello_Agents\hello-agents>
```

---

## 📦 已安装的依赖

核心依赖已安装完成：

- ✅ hello-agents 0.2.8
- ✅ openai 1.109.1
- ✅ numpy 2.3.4
- ✅ pandas 2.3.3
- ✅ python-dotenv 1.2.1
- ✅ huggingface_hub 1.1.2
- ✅ requests, httpx, pydantic 等

---

## 🔧 安装其他依赖

### 方式 1: 分步安装（推荐）

```powershell
# 确保虚拟环境已激活
.\venv_hello_agents\Scripts\Activate.ps1

# 安装扩展依赖
pip install -r requirements-step2.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 安装 Agent 框架（第6章需要）
pip install -r requirements-step3.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 安装可选依赖（Jupyter、测试等）
pip install -r requirements-step4.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 方式 2: 直接使用虚拟环境的 Python

```powershell
# 不激活虚拟环境，直接使用
.\venv_hello_agents\Scripts\python.exe -m pip install package_name
```

---

## 🧪 运行测试

### 快速测试

```powershell
# 方式 1: 激活虚拟环境后运行
.\venv_hello_agents\Scripts\Activate.ps1
python test/test_quick.py

# 方式 2: 直接指定 Python 解释器
.\venv_hello_agents\Scripts\python.exe test/test_quick.py
```

### 完整测试

```powershell
.\venv_hello_agents\Scripts\python.exe test_installation.py
```

---

## 📝 配置 API 密钥

1. **复制环境变量模板**（如果还没有 .env 文件）：
   ```powershell
   copy .env.example .env
   ```

2. **编辑 `.env` 文件**，填入您的 API 密钥：
   ```env
   # Moonshot AI 配置示例
   OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
   OPENAI_BASE_URL=https://api.moonshot.cn/v1
   OPENAI_MODEL=moonshot-v1-128k
   ```

   详细配置方法请参考：`国产大模型API配置指南.md`

---

## 💻 运行项目代码

### 方式 1: 激活虚拟环境后运行

```powershell
# 1. 激活虚拟环境
.\venv_hello_agents\Scripts\Activate.ps1

# 2. 进入代码目录
cd code/chapter1

# 3. 运行代码
python FirstAgentTest.py
```

### 方式 2: 直接使用虚拟环境的 Python

```powershell
# 直接指定 Python 解释器
.\venv_hello_agents\Scripts\python.exe code/chapter1/FirstAgentTest.py
```

---

## 🔍 检查虚拟环境

### 查看已安装的包

```powershell
.\venv_hello_agents\Scripts\python.exe -m pip list
```

### 查看特定包

```powershell
.\venv_hello_agents\Scripts\python.exe -m pip show hello-agents
```

### 检查 Python 版本

```powershell
.\venv_hello_agents\Scripts\python.exe --version
```

---

## 🐛 常见问题

### Q1: 提示 "无法加载文件，因为在此系统上禁止运行脚本"

**解决方案**:
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Q2: 激活虚拟环境后，pip 安装的包不在虚拟环境中

**解决方案**:
- 确保虚拟环境已激活（提示符前有 `(venv_hello_agents)`）
- 或直接使用 `.\venv_hello_agents\Scripts\python.exe -m pip install`

### Q3: 如何退出虚拟环境？

**解决方案**:
```powershell
deactivate
```

### Q4: 虚拟环境占用空间太大

**解决方案**:
- 虚拟环境通常占用 200-500MB
- 如果不需要，可以删除整个 `venv_hello_agents` 文件夹
- 需要时重新创建：`python -m venv venv_hello_agents`

---

## 📚 推荐工作流

### 日常使用

```powershell
# 1. 激活虚拟环境
.\venv_hello_agents\Scripts\Activate.ps1

# 2. 运行代码或测试
python your_script.py

# 3. 退出虚拟环境（可选）
deactivate
```

### 在 IDE 中使用

**VS Code**:
1. 打开项目文件夹
2. 按 `Ctrl+Shift+P`，输入 "Python: Select Interpreter"
3. 选择 `.\venv_hello_agents\Scripts\python.exe`

**PyCharm**:
1. File → Settings → Project → Python Interpreter
2. 添加新的解释器，选择 `venv_hello_agents\Scripts\python.exe`

---

## ✅ 验证安装

运行以下命令验证一切正常：

```powershell
# 测试导入
.\venv_hello_agents\Scripts\python.exe -c "from hello_agents import HelloAgentsLLM; print('✅ 安装成功!')"

# 运行快速测试
.\venv_hello_agents\Scripts\python.exe test/test_quick.py

# 运行完整测试
.\venv_hello_agents\Scripts\python.exe test_installation.py
```

---

## 🎯 下一步

1. ✅ 虚拟环境已配置完成
2. 📝 配置 API 密钥（参考 `国产大模型API配置指南.md`）
3. 🚀 开始学习（参考 `快速开始.md`）

**祝学习愉快！** 🎉

